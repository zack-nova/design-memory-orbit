# Design Memory 文档索引

从 `AGENTS.md` 进入本索引，再按任务递归读取对应子文档。

## 核心规则

1. `01-memory-model.md`：design memory 的职责、边界与安装形态。
2. `02-context-rules.md`：`CONTEXT.md` 与 `CONTEXT-MAP.md` 的格式、创建时机和维护规则。
3. `03-adr-rules.md`：ADR 格式、编号、状态、创建时机和冲突处理。
4. `04-safety-rules.md`：禁止事项、lazy create、冲突处理和不确定性处理。

## 模板

- `templates/context.template.md`
- `templates/adr.template.md`
